	元类在框架和库的作用
		验证属性
		一次把装饰器附到多个方法上
		序列化对象或转换数据
		对象关系映射
		基于对象的持久存储
		动态转换使用其他语言编写的类结构
		
	类对象
		cls.__bases__
			类的基类组成的元组
		
		cls.__qualname__
			类或函数的限定名称，包括类的全局作用域，包括继承关系
		
		cls.__subclass__
			返回一个包含类的直接子类的列表，弱引用，并且是i内存中的现存子类
		
		cls.mro()
			超类元组
		
	完